/*********************
Question:
	Write a function that takes a string as input and returns the string reversed.

Thought:
	Output the original string reversely to another string variable


**********************/

class Solution {
		public:
				string reverseString(string s) {
						string reverse="";

						for(int i = s.length()-1 ; i >= 0  ; i--){

								reverse+=s[i];
						}
						return reverse;
				}
};
